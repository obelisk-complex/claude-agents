# Claude Code Agents

[![Quillx](https://raw.githubusercontent.com/qainsights/Quillx/main/badges/quillx-4.svg)](https://github.com/qainsights/Quillx) Agent specifications and prompts were generated by Claude under direction. The scope, purpose, security model, and review priorities for each agent were defined by a human. Outputs were reviewed and iteratively refined through real-world use on production codebases.

A collection of specialised [Claude Code](https://code.claude.com/docs/en/overview) sub-agents for requirements engineering, code auditing, QA, security review, performance analysis, web performance auditing, test strategy, platform compatibility testing, accessibility, SEO, design, and red-team assessment.

## Agents

### Requirements and Planning

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [interview](agents/interview.md) | Structured interview to extract requirements; produces an actionable spec file | Write (spec file) | Project |
| [requirements-auditor](agents/requirements-auditor.md) | Audits specs for completeness gaps, missing NFRs, unstated assumptions | Read-only | Project |
| [migration-planner](agents/migration-planner.md) | Framework upgrades, large refactors, breaking-change planning | Read-only | Project |
| [plan-auditor](agents/plan-auditor.md) | Adversarial review of implementation and migration plans before execution | Read-only | Project |
| [conformance-auditor](agents/conformance-auditor.md) | Verifies an implementation matches its source of truth (spec, contract, README, tests); produces a traceability gap report | Read-only | Project |

### Code Quality and Review

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [code-auditor](agents/code-auditor.md) | Security vulnerabilities, anti-patterns, code quality; five-axis review framework | Read-only | Project |
| [security-auditor](agents/security-auditor.md) | Dedicated security review: OWASP Top 10, LLM Top 10, threat modelling, hardening | Read-only | Project |
| [pr-reviewer](agents/pr-reviewer.md) | Pull-request review with CI checks and contextual code reading | Read-only | Project |
| [perf-analyst](agents/perf-analyst.md) | Bottleneck identification, profiling, targeted optimisation; Core Web Vitals targets | Read-only | Project |
| [web-performance-auditor](agents/web-performance-auditor.md) | Web performance audits: Core Web Vitals, Lighthouse, CrUX, DevTools traces | Read-only | Project |
| [dependency-auditor](agents/dependency-auditor.md) | CVE scanning, licence review, supply-chain risk | Read-only | Project |
| [pre-release](agents/pre-release.md) | Pre-release sweep for debug leftovers, version mismatches, cruft | Read-only | Project |

### Testing

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [qa-agent](agents/qa-agent.md) | Test writing, test execution, browser testing via Playwright; prove-it pattern for bugs | Worktree (isolated writes) | Project |
| [test-engineer](agents/test-engineer.md) | Test strategy design, coverage gap analysis, scenario coverage matrix | Write | Project |
| [coverage-analyst](agents/coverage-analyst.md) | Coverage assessment, dead-code detection, gap prioritisation | Write | Project |
| [regression-test](agents/regression-test.md) | Pins existing behaviour with characterisation tests before changes | Write | Project |
| [integration-test](agents/integration-test.md) | Tests component interactions and service boundaries against real interfaces | Write | Project |
| [fuzz-test](agents/fuzz-test.md) | Crash-safety verification for code that parses external input | Write | Project |
| [mutation-test](agents/mutation-test.md) | Measures test-suite effectiveness; surfaces weak or missing assertions | Write | Project |

### Platform and Compatibility

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [compat-auditor](agents/compat-auditor.md) | General-purpose platform compatibility for any project | Read-only | Project |
| [ci-auditor](agents/ci-auditor.md) | CI/CD workflow security, efficiency, correctness, platform compat | Read-only | Project |

### Accessibility, SEO, and Design

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [a11y-auditor](agents/a11y-auditor.md) | WCAG 2.2 Level AA accessibility audit for web frontends | Read-only | Project |
| [seo-auditor](agents/seo-auditor.md) | SEO audit covering crawlability, on-page, structured data, Core Web Vitals indicators | Read-only | Project |
| [visual-hygiene](agents/visual-hygiene.md) | Spacing, typography, colour sprawl, dead UI, z-index chaos, design-token drift | Read-only | Project |
| [visual-flair](agents/visual-flair.md) | Opportunities for personality and delight - the "understated bombast" that elevates a clean site | Read-only | Project |
| [anti-ai-design](agents/anti-ai-design.md) | Audits frontends for patterns that make websites look AI-generated | Read-only | Project |
| [svg-illustrator](agents/svg-illustrator.md) | Hand-authored SVG illustrations - covers, figures, icons, diagrams - for PDF/print pipelines | Write | Project |
| [stitch-designer](agents/stitch-designer.md) | Generates screen layouts and component variants from design briefs | Write | Project |
| [copywriter](agents/copywriter.md) | Writes, audits, and revises web and marketing copy with a mandatory 7-pass self-editing loop | Read-only | Project |

### Red Team (Security Testing)

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [rt-recon](agents/rt-recon.md) | Attack-surface reconnaissance, subdomain enumeration, tech fingerprinting | Read-only | Project |
| [rt-injection](agents/rt-injection.md) | SQL injection, NoSQL injection, SSTI, OS command injection, XXE | Read-only | Project |
| [rt-xss](agents/rt-xss.md) | Reflected, stored, and DOM-based XSS with CSP bypass assessment | Read-only | Project |
| [rt-ssrf](agents/rt-ssrf.md) | Server-side request forgery, cloud-metadata access, filter bypass | Read-only | Project |
| [rt-auth-session](agents/rt-auth-session.md) | Authentication bypass, session management, JWT/token security | Read-only | Project |
| [rt-access-control](agents/rt-access-control.md) | IDOR, privilege escalation, forced browsing, HTTP verb tampering | Read-only | Project |
| [rt-api-abuse](agents/rt-api-abuse.md) | Rate limiting, mass assignment, data exposure, GraphQL introspection | Read-only | Project |
| [rt-business-logic](agents/rt-business-logic.md) | Payment-flow abuse, race conditions, workflow bypass, state-machine flaws | Read-only | Project |
| [rt-tls-headers](agents/rt-tls-headers.md) | TLS configuration, security headers, CORS policy, cookie flags | Read-only | Project |
| [rt-cache-poisoning](agents/rt-cache-poisoning.md) | Web cache poisoning, cache deception, cache-key manipulation | Read-only | Project |
| [rt-request-smuggling](agents/rt-request-smuggling.md) | HTTP request smuggling, CL/TE desync, H2 downgrade, WebSocket abuse | Read-only | Project |
| [rt-cloud-infra](agents/rt-cloud-infra.md) | Cloud storage exposure, subdomain takeover, DNS misconfiguration, deployment artefacts | Read-only | Project |

### Travel Research

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [travel-research-coordinator](agents/travel-research-coordinator.md) | Orchestrates travel-research agents; compiles a structured guide with PDF | Write (report + PDF) | Project |
| [local-insights-researcher](agents/local-insights-researcher.md) | Authentic local hidden gems and off-the-beaten-path experiences | Read-only | Project |
| [culinary-researcher](agents/culinary-researcher.md) | Local food, drink, wine, and spirits recommendations | Read-only | Project |
| [mainstream-attractions-researcher](agents/mainstream-attractions-researcher.md) | Mainstream tourist attractions, clearly labelled as tourist-friendly | Read-only | Project |
| [itinerary-compiler](agents/itinerary-compiler.md) | Day-by-day itinerary with timed activities and local tips | Read-only | Project |
| [travel-guide-designer](agents/travel-guide-designer.md) | Layout specification and PDF travel guide via ReportLab | Write (PDF) | Project |

### Meta

| Agent | Purpose | Mode | Memory |
|-------|---------|------|--------|
| [agent-auditor](agents/agent-auditor.md) | Audits and updates other agents against current best practices | Read-write | User |
| [blind-spot-auditor](agents/blind-spot-auditor.md) | Finds domain gaps, missing attack vectors, and coverage holes in agent definitions | Read-only | User |
| [token-usage-auditor](agents/token-usage-auditor.md) | Finds agent and skill text that spends context without buying signal | Read-only | User |
| [fix-regression-checker](agents/fix-regression-checker.md) | Verifies applied fixes still achieve their intent, not merely that their text survives | Read-only | Project |
| [skill-auditor](agents/skill-auditor.md) | Audits skills against the skill checklist, including packaging that decides whether a skill loads at all | Read-write | User |
| [skill-trigger-auditor](agents/skill-trigger-auditor.md) | Predicts whether a skill's description will fire on the situations it covers, and on ones it should not | Read-only | User |

## Installation

Copy the agent files you want into your project's `.claude/agents/` directory:

```bash
# Copy all agents
mkdir -p .claude/agents
cp agents/*.md .claude/agents/

# Or copy specific ones
cp agents/code-auditor.md .claude/agents/
cp agents/qa-agent.md .claude/agents/
```

For user-scope agents (available in every project), copy into `~/.claude/agents/` instead. Claude Code automatically discovers agents in either location.

## Usage

```bash
# Natural language in a Claude Code session
"Use the code-auditor agent to review src/"

# @-mention
@code-auditor review the auth module

# Session-wide (the agent handles everything)
claude --agent code-auditor
```

When invoked from the main conversation, Claude Code spawns agents as sub-agents with their own context window. Results are returned to the parent conversation.

## Security Model

- **Read-only by role, enforced by `disallowedTools` - not by `permissionMode`.** Most agents declare `permissionMode: plan`, which states read-only *intent*. Plan mode is not an enforcement boundary: tested 2026-07-18, a plan agent created and edited files. Enforcement comes from `disallowedTools: Write, Edit`, carried by every one of the 47 plan agents, which removes those tools at the API level and holds even under a permissive parent session.
- **`Bash` is an open write channel, so it is kept sparingly.** `disallowedTools` blocks the Write and Edit tools but not a shell redirect, so `Bash` was removed from the plan agents that never run a command (they search via `Grep`/`Glob`) and kept only where an agent genuinely executes something - the red-team probes, the tool-runners, the retrieval chains. Agents that keep `Bash` can still write via the shell; that residual is contained by the boundary below, not by their frontmatter.
- **The real integrity boundary is review before merge.** No agent-touched file should reach your main branch without a diff you have read. A stray working-tree write is recoverable; an unreviewed merge is not. Do not treat any single frontmatter field as a write-jail. The OS sandbox can scope writes but is session-wide, not per-agent, and fails open without `socat`, so it is a weak per-agent guard.
- **19 write-capable agents** (`permissionMode: acceptEdits`) legitimately write spec files, tests, reports, or agent definitions: `interview`, `qa-agent`, the `*-test` agents, `coverage-analyst`, the `travel-*` writers, `stitch-designer`, `agent-auditor`, `skill-auditor`, and the four new meta auditors among them.
- **qa-agent uses worktree isolation.** It has `acceptEdits` but operates in a temporary git worktree, so its changes branch off and do not touch your working tree until you review and merge them.
- **No MCP dependencies.** Fleet agents use only built-in Claude Code tools (Read, Write, Edit, Grep, Glob, Bash, WebSearch, WebFetch, Agent). External MCP servers are treated as threat vectors and excluded by design; `mcpServers:` is banned fleet-wide and gated by `scripts/check-agent-frontmatter.sh`.
- **Web tools.** Agents with `WebSearch` / `WebFetch` can query CVE databases, documentation, and known-issue trackers. Each web-using agent is instructed to consult a local project knowledge base first (e.g. `llm-wiki/`, `wiki/`, `docs/research/`) before fetching externally, and to redact project-specific identifiers from queries.
- **Memory.** Agents with `memory` enabled accumulate knowledge across conversations in `.claude/agent-memory/` (project scope) or `~/.claude/agent-memory/` (user scope).

## Agent Design Principles

- **Find real problems, not style nits.** Every finding should be something that matters.
- **Concrete fixes, not vague suggestions.** Include file paths, line numbers, and code snippets.
- **Severity classification.** Critical/High/Medium/Low/Info so teams can prioritise.
- **No manufactured findings.** If the code is clean, say so.
- **Aggressive verification.** Agents cross-check against CVE databases, current documentation, and the project's own prior research before reporting.
- **Domain principles plus cross-fleet principles.** Every agent's `Guiding Principles` section splits domain-specific rules from cross-fleet rules (warnings are errors, leave no trash, do the harder fix, secure by default) so behaviour stays consistent across the fleet.
- **Findings land on disk as they are found.** Long-running agents append to a report file rather than batching a final message, so a run that dies mid-pass still leaves its work behind. See `REPORT_PROTOCOL.md`.

## Model Variants

Each agent's base file carries the model its job needs. That is a design-time property: it follows from the kind of reasoning the agent does, and it is checked against `docs/model-tiers.tsv` by `scripts/check-agent-frontmatter.sh`. The variant directories are a separate, deployment-time mechanism: they let you run a given agent with more or less depth than its default on a particular job.

Tier criteria, so the next person tiers consistently:

| Tier | The agent's work is |
|------|---------------------|
| `haiku` | Comparison against an explicit, enumerated standard, where every finding is a mismatch someone could point at. No absence-detection, no reasoning about whether a mechanism achieves an intent. |
| `sonnet` | Domain reasoning over code or prose against a stated target, where the target exists but mapping it onto the artefact needs judgement. The default: anything you cannot confidently place in the other two tiers belongs here. |
| `opus` | Naming what is *not* there, or judging whether a mechanism achieves its intent rather than whether text matches, or synthesising across several artefacts. |

The commonest error is demoting an agent whose checklist looks mechanical but whose individual items are themselves absence-detection ("flag any user-facing commit missing from the changelog", "flag CSS classes defined but never used"). Read the workflow steps, not the agent's summary.

A fourth disqualifier sits outside the three criteria, because they measure kinds of reasoning and this one measures prose: an agent that must judge the register of its own output belongs on `sonnet` or above, whatever its workflow looks like. The case that found this gap was `mainstream-attractions-researcher`, whose steps are pure field extraction but whose guiding principles carry an anti-exoticisation standard - no Orientalist register, no monolithic cultural claims. Every step passed the haiku test and the agent still failed it. Where the cost of a wrong demotion is output that flattens a culture rather than output that misses a finding, leave the default alone.

| Directory | Model | Use when |
|-----------|-------|----------|
| `agents/` (default) | Per `docs/model-tiers.tsv` | Standard use. |
| `agents/opus-variants/` | `opus` | Maximum reasoning depth is worth the cost (available for `agent-auditor`, `blind-spot-auditor`, `conformance-auditor`, `interview`, `migration-planner`, `plan-auditor`, `requirements-auditor`, `travel-research-coordinator`). |
| `agents/sonnet-variants/` | `sonnet` | Scoped variants with tighter turn/effort budgets (available for `agent-auditor`, `blind-spot-auditor`, `migration-planner`). |

To switch an agent to a variant, copy the variant file over the default:

```bash
# Example: use the Opus variant of conformance-auditor
cp agents/opus-variants/conformance-auditor-opus.md agents/conformance-auditor.md
```

Do this in your own `.claude/agents/`, not in this repo: overwriting a base file here puts two agents under one name and `scripts/check-agent-frontmatter.sh` will fail.

## Customisation

Each agent is a Markdown file with YAML frontmatter. You can:

- Adjust `maxTurns` to give agents more or less time.
- Change `model` (`sonnet`, `opus`, `haiku`) to control depth vs cost.
- Set `effort` to `low`, `medium`, `high`, or `max` to control reasoning depth.
- Add `memory` (`user`, `project`, or `local`) for cross-session learning.
- Set `color` for visual identification in the UI.
- Add `skills` to preload domain knowledge.
- Add project-specific context to the prompt body.

## Licence

[GPL-3.0-or-later](LICENSE)
